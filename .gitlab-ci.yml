stages:
  - build
  - test
  - deploy

build-job:
  stage: build
  script:
    - echo "Running the build job..."
    - mkdir build_artifacts
    - touch build_artifacts/my_app.exe # Simulate a build output